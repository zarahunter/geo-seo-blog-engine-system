# Research artifacts

The weekly sprint writes its outputs here:
- `keyword-universe.csv` — the canonical topic/keyword database (headers ready).
- SERP-difficulty audits and AI-citation scans (one file per scan).
